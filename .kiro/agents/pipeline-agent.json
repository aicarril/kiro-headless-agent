{
  "name": "pipeline-agent",
  "description": "Orchestrates the full pipeline: clones repo, delegates implementation to code-agent, delegates review to pr-reviewer-agent, handles fix loop. All in one run.",
  "prompt": "file://./prompts/pipeline-agent.md",
  "tools": ["read", "write", "shell", "grep", "glob", "subagent"],
  "allowedTools": ["read", "write", "shell", "grep", "glob", "subagent"],
  "toolsSettings": {
    "subagent": {
      "availableAgents": ["code-agent", "pr-reviewer-agent"],
      "trustedAgents": ["code-agent", "pr-reviewer-agent"]
    }
  }
}
